# vivo-linkkit — exploration dependencies (P1–P2)
# System tools (jadx, 7z, tshark) are installed via scripts/bootstrap.sh.

websockets         # the phone link is wss://<ip>:<port> JSON (PROTOCOL.md §2/§3)
cryptography       # aes-256-cbc session cipher + key/iv derivation (§4)
requests           # the phone's HTTP file APIs (§2)
qrcode             # generate/parse the pairing QR payload (§1/§5)
playwright         # drive the user's vivo passport login in a browser (§8, login.py)
                   #   after install:  playwright install chromium
