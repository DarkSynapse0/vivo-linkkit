# vivo-linkkit dependencies.  Prefer installing the package:
#   pip install -e .            # core client  (websockets)
#   pip install -e '.[login]'   # + playwright, for `vivolinkkit login`
# System tools (adb, jadx, 7z, tshark) come from scripts/bootstrap.sh.

websockets>=12     # control-plane websocket to the phone (PROTOCOL.md §1)
playwright>=1.40   # `vivolinkkit login` — drives your own vivo passport login
                   #   after install:  playwright install chromium

# Instrumentation only (observing the official client — see scripts/vm/):
mitmproxy          # decrypt the account/cloud + phone TLS while capturing
